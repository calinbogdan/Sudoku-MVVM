<UserControl x:Class="Sudoku.SlotView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             x:Name="slotView"
             xmlns:local="clr-namespace:Sudoku"
             mc:Ignorable="d">
    <DockPanel Height="50" Width="50">
        <TextBox Text="{Binding SlotValue}" TextAlignment="Center" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="24" FontWeight="Bold" RenderTransformOrigin="0.5,0.5" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
    </DockPanel>
</UserControl>
